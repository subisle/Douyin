# 项目规则（开发与产品硬约束）

本文件约束本仓库的实现与文档表述。与 `docs/` 冲突时：**本文件 + 运行代码 > 过时设计段落**。

## 1. 用户通道（冻结）

1. **唯一用户会话通道：微信 iLink**（私聊 / 群聊）。
2. **所有面向最终用户的能力**必须能通过 iLink 完成，包括但不限于：
   - 收发文字消息  
   - 接收文件（如 CSV 导入）  
   - 发送文件 / 图片（如日报图、音浪 CSV 导出）  
3. **禁止**将以下作为最终用户主聊天通道：
   - 公众号、企业微信、通用 Webhook IM、飞书/钉钉等  
   - Web 页面聊天作为主入口（`/agent` 等仅管理/诊断）  
4. 协议底层若使用微信官方上传/下载地址，仍视为 **iLink 协议内收发**，不是「另建用户 CDN/文件站通道」。
5. **禁止**桌面 Electron 与服务器 `bot-worker` **同时**运行**同一**微信 iLink 账号。

## 2. 智能形态（冻结）

1. 用户侧智能为 **恰好一个 AI Agent + 多个技能（Tools）**。
2. **一技能一功能**；扩展 = 新增技能，不是新增 Agent。
3. **禁止**把多 Agent / Crew 作为微信实时问答默认路径。
4. 真值规格：`docs/superpowers/specs/2026-07-24-weixin-single-agent-skills-design.md`。

## 3. 数据与安全

1. 音浪、时长、排名等数字 **只** 来自数据库与结构化技能；禁止模型编造。
2. **禁止**用 RAG/知识库片段冒充实时业务数字。
3. 不向用户输出 API Key、Token、数据库密码、未脱敏内部路径。

## 4. 服务器与生产

1. `scripts/bot-worker` 等服务器能力在未完成路线图 S7 前 **不得**标记「服务器生产可用」。
2. 服务器实验不改变第 1、2 节产品形态。

## 5. 文档与提交

1. 改通道/Agent 形态时，同步更新本文件与单 Agent 规格。
2. 不要把无关桌面业务脏改动与 bot/iLink 提交混在一起。
