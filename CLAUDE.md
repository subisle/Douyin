# 项目规则（开发与产品硬约束）

本文件约束本仓库的实现与文档表述。与 `docs/` 冲突时：**本文件 + 运行代码 > 过时设计段落**。

## 1. 用户通道（冻结）

1. **用户会话通道（官方）**：
   - **主通道：微信 iLink**（私聊 / 群聊）
   - **附加通道：QQ 开放平台官方机器人**（群 @ / 私聊 C2C；桌面 `qq-bot`）
2. **所有面向最终用户的能力**应优先能在上述官方通道完成，包括但不限于：
   - 收发文字消息  
   - 接收文件（如 CSV 导入；QQ 侧能力以开放平台支持为准，可分阶段）  
   - 发送文件 / 图片（如日报图、音浪 CSV、PK 分组图）  
3. **禁止**将以下作为最终用户主聊天通道：
   - 公众号、企业微信、通用 Webhook IM、飞书/钉钉等  
   - 非官方 QQ 协议（NapCat / OneBot 逆向客户端等）作为默认产品通道  
   - Web 页面聊天作为主入口（`/agent` 等仅管理/诊断）  
4. 协议底层若使用微信 / QQ 官方上传/下载地址，仍视为 **对应官方协议内收发**，不是「另建用户 CDN/文件站通道」。
5. **禁止**桌面 Electron 与服务器 `bot-worker` **同时**运行**同一**微信 iLink 账号。
6. 微信与 QQ 共用 **同一套指令处理器**（`createWeixinCommandHandler`）；差异只在 Transport / 身份（openid）/ 媒体能力，不复制业务实现。
7. QQ 支持 **多机器人**：一份配置（`data/builtin/qq-bots/*.json`，字段 `appId` / `clientSecret` / 可选 `label`）= 一个实例，
   各自独立连接与状态；同一 appId 只允许一份配置生效。

## 2. 智能形态（冻结 · 2026-09-18 变更）

1. **本项目不提供 AI 对话能力**：没有模型调用、没有 Agent、没有知识库（RAG）、没有会话记忆。
2. 用户侧只支持 **固定指令 + 确定性技能**（查询 / 出图 / 导出 / 导入 / 绑定 / 推送开关）。
3. **禁止**重新引入 LLM 调用、`AI_*` / `OPENAI_*` 环境变量、多 Agent 编排；需要新能力时**新增确定性指令**。
4. 用户发已下线的 AI 口令（人工客服 / 智能模式 / 清空对话等）时，机器人回固定说明并引导到「帮助」。
5. 变更历史：本分支（`3318`）移除了完整的单 Agent + 技能实现；历史规格
   `docs/superpowers/specs/2026-07-24-weixin-single-agent-skills-design.md` 已标注废弃，仅作存档。
6. 回归闸门：`electron/weixin-bot-security.test.js` 中的「AI 能力已整体移除」用例会校验
   模块/路由/环境变量/`agentHandler` 均不得复活。

## 3. 数据与安全

1. 音浪、时长、排名等数字 **只** 来自数据库与结构化技能；**禁止**任何形式的内容编造。
2. **禁止**用静态文案冒充实时业务数字（例如把示例数字写进回复模板）。
3. 不向用户输出 API Key、Token、数据库密码、未脱敏内部路径。

## 4. 服务器与生产

1. `scripts/bot-worker` 等服务器能力在未完成路线图 S7 前 **不得**标记「服务器生产可用」。
2. 服务器实验不改变第 1、2 节产品形态。

## 5. 文档与提交

1. 改通道 / 指令形态时，同步更新本文件与相关规格文档。
2. 不要把无关桌面业务脏改动与 bot/iLink 提交混在一起。
3. 新增机器人能力必须有对应测试；删除能力要同时清理测试、门禁脚本与文档表述。
